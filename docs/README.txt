This directory contains documentation that can be built by the mkdocs tool
into a website.

It is intended that the older documentation from the `doc/` directory will be
refactored into here as it is incorporated into the mkdocs framework.
